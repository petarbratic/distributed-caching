global:
  scrape_interval: 1s

scrape_configs:
  - job_name: "gateway"
    static_configs:
      - targets:
          - gateway:8080

  - job_name: "backend"
    static_configs:
      - targets:
          - backend:8081